# youtube-clone

Create React App setup for a YouTube clone (MUI, react-player, react-router, axios). Only the scaffold is here, the clone itself was never built.

```sh
npm install
npm start
```

Not maintained.
